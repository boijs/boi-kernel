'use strict'

let config = require('./_config.js');
let plugin = require('./_plugin.js');
let server = require('./_server.js');
let env = require('./_env.js');

module.exports = {
    config,
    plugin,
    server,
    env
};
